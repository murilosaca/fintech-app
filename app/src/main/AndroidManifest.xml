<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FintechApp"
        tools:targetApi="31">

        <!-- Activity de Extrato -->
        <activity
            android:name=".screens.ExtratoActivity"
            android:exported="false"
            android:parentActivityName=".screens.MainActivity" />

        <!-- Activity de Cadastro -->
        <activity
            android:name=".screens.CadastroActivity"
            android:exported="false"
            android:parentActivityName=".screens.MainActivity" />

        <!-- Activity Principal (Dashboard) - É a tela que abre o app -->
        <activity
            android:name=".screens.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
